spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: yeshua18
    url: jdbc:mysql://localhost:3308/fornecedor?useTimezone=true&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 
